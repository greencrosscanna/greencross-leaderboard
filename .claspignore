# Ignore everything except the GAS backend files.
# user_admin.gs is a separate Sheets-bound project — excluded intentionally.
**
!dutchie_proxy.gs
!auth.gs
!dutchie_fetch.gs
!discounts.gs
!goals.gs
!endpoints.gs
!gx_roster.gs
!cache.gs
!snapshot.gs
!tests.gs
!index.html
!appsscript.json
# TEMPORARY — the one-time store_id key migration. Remove this line and delete the file
# once migrateStoreKeysToStoreId() has run and the deploy is verified.
!migrate_store_keys.gs
!hourly_dist_repair.gs
